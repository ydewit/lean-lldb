{
    "version": "0.2.0",
    "configurations": [
        // This is for Microsoft's C/C++ extension
        {
            "name": "(lldb) Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/tests/main",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "lldb",
            "logging": {
                // "natvisDiagnostics": true,
                "engineLogging": true,
                // "trace": true,
                // "traceResponse": true
            }
        },
        // This is for codelldb
        {
            "name": "main (Debug)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/tests/main",
            "args": [],
            "cwd": "${workspaceFolder}"
        },
    ]
}